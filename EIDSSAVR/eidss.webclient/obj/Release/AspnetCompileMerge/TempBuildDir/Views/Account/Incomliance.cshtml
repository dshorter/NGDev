@{
    ViewBag.Title = Translator.GetMessageString("EIDSSIncomliance");
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

@using bv.common.Configuration
@using eidss.web.common.Utils

<script language="javascript" type="text/javascript">
    $(document).ready(function() {
        //defaultPage.incomliancePageOnLoad();
    });
</script>

@if (BaseSettings.TranslationMode)
{
    <input type='hidden' id='aspclassname' value='@GetType().FullName' />
}
<input type='hidden' id='urlToRedirect' name='urlToRedirect' value='@ViewBag.UrlToRedirect' />

<table class="timeout">
    <tr>
        <td colspan="2">
            <div class="timeoutMessage">
                @Translator.GetMessageString("msgSessionIncomliance1")
                <br/>
                @Translator.GetMessageString("msgSessionIncomliance2")
            </div>
        </td>
    </tr>
    <tr>
        <td style="width: 50%"></td>
        <td class="loginButtons">
            @if (BaseSettings.TranslationMode)
            {
                <input id="idButtonTranslation" type="button" class="button" value="@Translator.GetMessageString("btTranslationTool")" onclick="bvTranslation.OpenTranslationWindow();" />
            }
            <input id="idButtonIncomliance" type="submit" class="button" value="@Translator.GetMessageString("btnSessionIncomliance")"/>
        </td>   
    </tr>
</table>
