namespace eidss.model.Core
{
    public class EidssSiteOptions
    {
        // don't rename these properties because they names using in buinding
        public string strName { get; set; }
        public string strValue { get; set; }
    }
}